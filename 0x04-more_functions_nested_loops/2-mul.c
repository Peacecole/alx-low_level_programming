#include "main.h"
/**
 * mul - Return the product of two numbers
 * @num1: first integer type
 * @num2: second integer type
 * Return: product of two numbers
 */
int mul(int num1, int num2)
{
	return (num1 * num2);
}
