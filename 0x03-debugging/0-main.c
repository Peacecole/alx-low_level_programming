#include "main.h"
/**
 * main - entry point
 * tests function that prints if integer is positive or negative
 * Return: always 0
 */
int main(void)
{

	int x;

	x = 0;

	positive_or_negative(x);

	return (0);
}
