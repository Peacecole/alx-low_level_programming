#include <stdio.h>
/**
 * main - causes an ending loop
 * Return: always 0
 *
 */
int main(void)
{
	int i;

	printf("Infinite loop coming:(\n");

	i = 0;

	/**
	 * while(i < 10)
	 * {
	 * putchar(i);
	 * }
	 */

	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
